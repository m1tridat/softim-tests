@model IEnumerable<WebApplication.Dtos.VisitsBySexDto>
@{
    ViewBag.Title = "Statistics by sex";
}

<div class="filter-form">
    <div class="row">
        @using (Html.BeginForm("StatisticsBySex", "Home", FormMethod.Post, new {@class = "form-inline"}))
        {
            <div class="form-group">
                <label for="shopId">Select shop:</label>
                @Html.DropDownList("shopId", ViewBag.Shops as SelectList, "", new {@class = "form-control"})
            </div>
            <div class="form-group">
                <label for="date">Enter date: mm.dd.yyyy</label>
                @Html.TextBox("date", null, new {@class = "form-control datepicker"})
            </div>
            <div class="form-group">
                <input type="submit" value="Filter" class="btn btn-default"/>
            </div>
        }
    </div>
</div>

<div class="row">
    <h3>Statistics by sex</h3>
    <table class="table table-striped">
        <thead>
        <tr>
            <th scope="col">Shop</th>
            <th scope="col">Address</th>
            <th scope="col">Date</th>
            <th scope="col">Mans</th>
            <th scope="col">Womans</th>
        </tr>
        </thead>
        <tbody>
        @foreach (var v in Model)
        {
            <tr>
                <th scope="row">@v.ShopName</th>
                <td>@v.Address</td>
                <td>@v.Date.ToShortDateString()</td>
                <td>@v.Mans</td>
                <td>@v.Womans</td>
            </tr>
        }
        </tbody>
    </table>
</div>